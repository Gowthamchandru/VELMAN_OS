# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
**/node_modules/
dist
**/dist/
dist-ssr
*.local

# Secrets — never commit your Claude token
.env
.env.*
!.env.example

# OS / editor
.DS_Store
**/.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling/config
.claude/
.obsidian/

# Personal source materials at repo root (not code — kept out of the repo)
/*.xlsx
/*.HEIC
/*.heic

# Source artwork: 28MB original kept locally; the optimised
# velman-bg.webp/.jpg beside it are what the app ships.
app/public/images/Velman_bg.png

# Savery: personal-use-only demo licence (no commercial use, no
# redistribution). Kept locally; the app falls back to the bundled
# "Cyberpunk Is Not Dead" wherever these files are absent.
app/public/savery/

# Turbo Driver (SRON Studio): personal-use-only demo licence.
# Kept locally, never redistributed.
app/public/turbo-driver/

# Source artwork kept locally; the optimised velman-portrait.* beside
# it is what the app ships.
app/public/images/bg_image.png
app/public/images/1..png
app/public/images/bg_imag.png
app/public/images/bg_img.png

# Tried and dropped as the /mode backdrop, kept in case it comes back:
# the 618x341 pixel art and its 2x upscale. Nothing ships from these —
# /mode uses velman-bg.webp/.jpg, from Velman_bg.png above.
app/public/images/bg2.png
app/public/images/bg2 (1).png
