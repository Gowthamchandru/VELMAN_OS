# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
**/node_modules/
dist
**/dist/
dist-ssr
*.local

# Secrets — never commit your Claude token
.env
.env.*
!.env.example

# OS / editor
.DS_Store
**/.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling/config
.claude/
.obsidian/

# Personal source materials at repo root (not code — kept out of the repo)
/*.xlsx
/*.HEIC
/*.heic

# Source artwork: 28MB original kept locally; the optimised
# velman-bg.webp/.jpg beside it are what the app ships.
app/public/images/Velman_bg.png

# Savery: personal-use-only demo licence (no commercial use, no
# redistribution). Kept locally; the app falls back to the bundled
# "Cyberpunk Is Not Dead" wherever these files are absent.
app/public/savery/
